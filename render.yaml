services:
  - type: web
    name: cryptosim-web
    env: rust
    buildCommand: "cd frontend && npm install && npm run build && cd ../backend && cargo build --release"
    startCommand: "cd backend && cargo run --release"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cryptosim-db
          property: connectionString
